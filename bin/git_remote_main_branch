#!/usr/bin/env

remotes=( $(git branch -vv -r | grep -E '^\s+[^/]+/HEAD' | cut -d'>' -f2) )
remote_heads=()
if (( ${#remotes[@]} > 0 )); then
  for remote in "${$remotes[@]}"; do

  done
else
  false
fi
